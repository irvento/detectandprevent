<svg viewBox="0 0 100 100" fill="none" xmlns="http://www.w3.org/2000/svg" {{ $attributes }}>
  <defs>
    <radialGradient id="grad" cx="50%" cy="50%" r="50%">
      <stop offset="0%" stop-color="#f093fb" />
      <stop offset="100%" stop-color="#f5576c" />
    </radialGradient>
  </defs>
  <!-- Outer circle with gradient fill -->
  <circle cx="50" cy="50" r="48" fill="url(#grad)" stroke="#333" stroke-width="2" />
  <!-- Centered text. Adjust as needed for your application name or initials -->
  <text x="50" y="60" text-anchor="middle" font-family="Arial, sans-serif" font-size="36" fill="#fff" font-weight="bold">
    DP
  </text>
</svg>